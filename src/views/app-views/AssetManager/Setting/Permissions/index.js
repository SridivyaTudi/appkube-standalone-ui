import React, { Component } from "react";
import { Box, Grid } from "@mui/material";

export class Permissions extends Component {
  constructor(props) {
    super(props);
    this.state = {};
  }

  render() {
    const {} = this.state;
    return <Box className="permissions-container">Permissions</Box>;
  }
}
export default Permissions;
